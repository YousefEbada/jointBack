version: "3.9"
services:
  mongo:
    image: mongo:6
    restart: unless-stopped
    ports: ["27017:27017"]
    environment:
      MONGO_INITDB_DATABASE: joint_clinic_dev
    volumes:
      - mongo_data:/data/db
volumes:
  mongo_data:
